package com.acceso.wfcore.apis;

public class DataAPI extends GenericAPI {

    public String SQL(String connection, String query) {
        String response = null;
//consquier del servicio de conexiones la conexion
        //ejecutar la query sobre la conexion
        //resolver la conexion como un json
        return response;
    }
}
